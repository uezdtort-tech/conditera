# КОНСТРУКТОР ТОРТОВ — ПОЛНОЕ СТРУКТУРНОЕ ОПИСАНИЕ

**Компонент:** `src/components/cake-builder/cake-builder-dialog.tsx` (930 строк)
**Тип:** Модальное окно (Dialog из shadcn/ui)
**State management:** Zustand store (`useAppStore`) → в v2.0 Supabase (`useSubmitInquiry`)
**Размер:** `max-w-4xl max-h-[95vh]`
**Поведение:** 8-шаговый wizard с прогресс-баром

---

## 📐 АРХИТЕКТУРА КОМПОНЕНТА

### Слои (сверху вниз)

```
┌──────────────────────────────────────────────────┐
│  Dialog (max-w-4xl, max-h-95vh)                  │
│  ┌────────────────────────────────────────────┐  │
│  │  Background Image (/builder-bg.png)         │  │
│  │  + Semi-transparent overlay (bg-background/90)│
│  │  ┌──────────────────────────────────────┐  │  │
│  │  │  HEADER (shrink-0)                    │  │  │
│  │  │  - Cake icon (круглый, primary)       │  │  │
│  │  │  - Title: "Конструктор тортов"        │  │  │
│  │  │  - Badge: "Шаг N / 8"                 │  │  │
│  │  │  - Subtitle: STEP_LABELS[step]        │  │  │
│  │  │  - Close button (X)                   │  │  │
│  │  │  - Progress bar: 8 сегментов          │  │  │
│  │  └──────────────────────────────────────┘  │  │
│  │  ┌──────────────────────────────────────┐  │  │
│  │  │  CONTENT (flex-1, overflow-y-auto)    │  │  │
│  │  │  - step === 0 → Мероприятие          │  │  │
│  │  │  - step === 1 → Основа               │  │  │
│  │  │  - step === 2 → Начинка              │  │  │
│  │  │  - step === 3 → Покрытие             │  │  │
│  │  │  - step === 4 → Декор                │  │  │
│  │  │  - step === 5 → Диета                │  │  │
│  │  │  - step === 6 → Доставка             │  │  │
│  │  │  - step === 7 → Резюме + Кондитеры    │  │  │
│  │  │  - submitted → Success screen         │  │  │
│  │  └──────────────────────────────────────┘  │  │
│  │  ┌──────────────────────────────────────┐  │  │
│  │  │  FOOTER (shrink-0, border-t)         │  │  │
│  │  │  - Back button (ghost, disabled 0)   │  │  │
│  │  │  - "Шаг N из 8" / "Отправить N"     │  │  │
│  │  │  - Next/Submit button (primary)      │  │  │
│  │  └──────────────────────────────────────┘  │  │
│  └────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────┘
```

### State (Zustand store)

```typescript
interface CakeBuilderState {
  step: number;              // 0-7 (текущий шаг)
  eventType: string | null;  // 'birthday' | 'wedding' | 'kids' | 'corporate' | 'romantic' | 'just'
  base: string | null;       // 'sponge' | 'mousse' | 'cheesecake' | 'honey' | 'red_velvet' | 'biscuit'
  filling: string | null;    // 'condensed_milk' | 'cream_pleshir' | 'chocolate_ganache' | ...
  coating: string | null;    // 'cream_cheese' | 'ganache_white' | 'ganache_dark' | 'mirror_glaze' | ...
  decorations: string[];     // ['berries', 'chocolate_curls', 'macarons', ...] — мультивыбор
  dietary: string[];         // ['sugar_free', 'gluten_free', 'vegan', ...] — мультивыбор
  servings: number;          // 2-200 (по умолчанию 8)
  city: string | null;       // 'Москва', 'Тула', ...
  deliveryDate: string;      // '2026-08-20'
  deliveryType: string;      // 'delivery' | 'pickup' | 'self_pickup'
  address: string;           // 'ул. Тверская, д. 12, кв. 45'
  inscription: string;       // 'С днём рождения, Маша!' (max 50 символов)
  comment: string;           // особые пожелания
}
```

### Локальный state (внутри компонента)

```typescript
const [submitted, setSubmitted] = useState(false);
const [selectedConfectioners, setSelectedConfectioners] = useState<string[]>([]);
const [requestDiscount, setRequestDiscount] = useState(false);
const [discountPercent, setDiscountPercent] = useState(0);       // 0-30
const [discountComment, setDiscountComment] = useState("");
```

---

## 📋 ДЕТАЛЬНОЕ ОПИСАНИЕ КАЖДОГО ШАГА

### Шаг 0: Мероприятие (обязательно)

**Заголовок:** "Какое мероприятие?"
**Подзаголовок:** "Выберите тип события, чтобы мы подобрали подходящий торт"
**Layout:** Grid 2×3 (mobile) / 3×2 (desktop)

**Опции (6):**
| ID | Label | Icon | Description |
|---|---|---|---|
| `birthday` | День рождения | 🎂 | Праздничный торт для именинника |
| `wedding` | Свадьба | 💍 | Многоярусный торт для торжества |
| `kids` | Детский праздник | 🧸 | Яркий торт с любимыми героями |
| `corporate` | Корпоратив | 🏢 | Для офисного праздника |
| `romantic` | Романтический | 💕 | Торт для двоих |
| `just` | Просто так | 🍰 | Угостить себя и близких |

**Цена:** не влияет на стоимость
**canProceed:** `!!cakeBuilder.eventType`
**Визуал:** Card с emoji 3xl + label + description, клик → выбор

---

### Шаг 1: Основа (обязательно)

**Заголовок:** "Выберите основу торта"
**Подзаголовок:** "Это определит текстуру и вкус вашего торта"
**Layout:** Grid 1×N (mobile) / 2×N (desktop)

**Опции (6):**
| ID | Label | Description | Price modifier |
|---|---|---|---|
| `sponge` | Бисквит | Классический воздушный бисквит | +0 ₽ |
| `mousse` | Муссовый | Нежный мусс без выпечки | +400 ₽ |
| `cheesecake` | Чизкейк | На песочной основе | +300 ₽ |
| `honey` | Медовик | Тонкие медовые коржи | +0 ₽ |
| `red_velvet` | Красный бархат | Яркий бисквит со сливочным кремом | +200 ₽ |
| `biscuit` | Песочный бисквит | Рассыпчатая текстура | +0 ₽ |

**Цена:** добавляется `priceBase` к базовой цене (1500 ₽)
**canProceed:** `!!cakeBuilder.base`
**Визуал:** Card с label + description + Badge "+N ₽" (если priceBase > 0)

---

### Шаг 2: Начинка (обязательно)

**Заголовок:** "Начинка"
**Подзаголовок:** "Выберите любимую начинку для вашего торта"
**Layout:** Grid 1×N (mobile) / 2×N (desktop)

**Опции (8):**
| ID | Label | Price |
|---|---|---|
| `condensed_milk` | Варёная сгущёнка | +0 ₽ |
| `cream_pleshir` | Крем пломбир | +200 ₽ |
| `chocolate_ganache` | Шоколадный ганаш | +350 ₽ |
| `curd_cream` | Творожный крем | +250 ₽ |
| `raspberry` | Малиновый конфитюр | +280 ₽ |
| `mango_passion` | Манго-маракуйя | +400 ₽ |
| `strawberry` | Клубничный конфитюр | +300 ₽ |
| `lemon_curd` | Лимонный курд | +250 ₽ |

**Уникальная фича:** Каждый вариант отображается с **3D-превью среза торта** через `FillingSlicePreview` компонент (56px). Показывает визуализацию слоёв: бисквит + начинка + покрытие.

**Цена:** добавляется `price` к базовой
**canProceed:** `!!cakeBuilder.filling`
**Визуал:** Card с FillingSlicePreview + label + Badge "+N ₽"

---

### Шаг 3: Покрытие (обязательно)

**Заголовок:** "Покрытие"
**Подзаголовок:** "Внешний вид вашего торта"
**Layout:** Grid 1×N (mobile) / 2×N (desktop)

**Опции (6):**
| ID | Label | Price |
|---|---|---|
| `cream_cheese` | Крем-чиз | +0 ₽ |
| `ganache_white` | Белый ганаш | +200 ₽ |
| `ganache_dark` | Тёмный ганаш | +250 ₽ |
| `mirror_glaze` | Зеркальная глазурь | +350 ₽ |
| `velvet_spray` | Велюровое напыление | +400 ₽ |
| `mastic` | Сахарная мастика | +500 ₽ |

**Цена:** добавляется `price`
**canProceed:** `!!cakeBuilder.coating`
**Визуал:** Card с label (слева) + Badge "+N ₽" (справа)

---

### Шаг 4: Декор (опционально, мультивыбор)

**Заголовок:** "Декор"
**Подзаголовок:** "Можно выбрать несколько вариантов (опционально)"
**Layout:** Grid 2×N (mobile) / 3×N (desktop)

**Опции (8):**
| ID | Label | Price |
|---|---|---|
| `berries` | Свежие ягоды | +400 ₽ |
| `chocolate_curls` | Шоколадная стружка | +150 ₽ |
| `macarons` | Макаронс | +350 ₽ |
| `meringue` | Безе | +300 ₽ |
| `sugar_flowers` | Сахарные цветы | +800 ₽ |
| `fresh_fruit` | Свежие фрукты | +350 ₽ |
| `edible_gold` | Сусальное золото | +600 ₽ |
| `wafer_paper` | Вафельная бумага | +250 ₽ |

**Цена:** сумма всех выбранных `price`
**canProceed:** `true` (всегда — шаг опциональный)
**Визуал:** Card с label + Badge "+N ₽", клик → toggle (add/remove из массива decorations[])

---

### Шаг 5: Диета (опционально, мультивыбор)

**Заголовок:** "Диетические предпочтения"
**Подзаголовок:** "Учтём особые требования (опционально)"
**Layout:** Grid 1×N (mobile) / 2×N (desktop)

**Опции (6):**
| ID | Label | Description |
|---|---|---|
| `sugar_free` | Без сахара | Сахарозаменитель вместо сахара |
| `gluten_free` | Без глютена | Без пшеничной муки |
| `lactose_free` | Без лактозы | Растительные сливки |
| `vegan` | Веганский | Без животных продуктов |
| `keto` | Кето | Низкоуглеводный |
| `hypoallergenic` | Гипоаллергенный | Без орехов и яиц |

**Цена:** не влияет (но влияет на фильтрацию кондитеров — не все делают веганские торты)
**canProceed:** `true` (всегда)
**Визуал:** Card с label + description

---

### Шаг 6: Доставка (обязательно)

**Заголовок:** "Доставка"
**Подзаголовок:** "Где и когда вам нужен торт?"
**Layout:** Single column, max-w-2xl

**Поля:**

| Поле | Тип | Обязательное | Placeholder | Иконка |
|---|---|---|---|---|
| Город | Input (text) | ✅ | "Москва" | MapPin |
| Дата доставки | Input (date) | ✅ | — | Calendar |
| Способ получения | OptionCard × 2 | ✅ | — | Truck / User |
| Адрес доставки | Input (text) | если delivery | "ул. Тверская, д. 12, кв. 45" | — |
| Количество порций | Input (number, 2-200) | ✅ | "10" | — |

**Способы получения:**
- **Доставка** (Truck icon) — стоимость рассчитывается через `calculateDelivery(price)`
  - Бесплатно при заказе от 3000 ₽
  - Иначе: 300 ₽ базово + 50 ₽/км
- **Самовывоз** (User icon) — бесплатно

**Порции:**
- Базовая цена = 1500 ₽ за 8 порций
- Дополнительно: +180 ₽ за каждую порцию свыше 8
- Например: 15 порций = 1500 + (15-8) × 180 = 1500 + 1260 = 2760 ₽

**canProceed:** `!!city && !!deliveryDate && !!deliveryType`

---

### Шаг 7: Резюме + Кондитеры (обязательно)

**Заголовок:** "Выберите кондитеров"
**Подзаголовок:** "Выберите одного или нескольких кондитеров в городе [city] для отправки запроса."

**Layout:** Single column, max-w-2xl, 4 секции:

#### 7.1. Сводка заказа (компактная)

Card с:
- **3D-превью среза** (FillingSlicePreview, 64px) — визуализация выбранной начинки
- Inline-сводка: Мероприятие · Основа · Начинка · Покрытие · Порций · Дата
- Надпись (если есть)
- Комментарий (если есть)
- Оценочная стоимость: **~{formatCurrency(estimatedPrice + deliveryCost)}**

#### 7.2. Надпись и комментарий (опционально)

| Поле | Тип | Max | Placeholder |
|---|---|---|---|
| Надпись на торте | Input | 50 символов | "С днём рождения, Маша!" |
| Комментарий кондитеру | Textarea (2 rows) | — | "Особые пожелания, цветовое решение, аллергии..." |

#### 7.3. Запрос скидки (опциональный блок)

Жёлтый блок (border-amber-300, bg-amber-50/50) с:
- **Checkbox:** "Запросить скидку" (Percent icon)
- При активации:
  - **Range slider** (0-30%) с Badge "{discountPercent}%"
  - **Input** "Комментарий к запросу скидки"
  - Hint: "Кондитеры увидят ваш запрос скидки и могут предложить скидку или отказаться"

#### 7.4. Список кондитеров (мультивыбор)

**Фильтрация:**
- Кондитеры в выбранном городе (`c.city === cakeBuilder.city`)
- ИЛИ кондитеры, доставляющие в этот город (`deliveryCities.includes(city)`)
- Fallback: кондитеры из Москвы (если в городе нет своих)

**Каждая карточка кондитера:**
- Аватар (40×40, rounded-full)
- businessName + verified badge (✓)
- Рейтинг: ⭐ 4.9 (234 отзыва) · 1248 заказов · Москва
- Специализация: "торты · капкейки · свадебные"
- Чек-маркер (круг, справа) —选中 = primary, не选中 = border

**Badge сверху:** "Кондитеры в г. [city] (N)" + "Выбрано: M"

**canProceed:** `selectedConfectioners.length > 0`

#### 7.5. Оценочная стоимость (итог)

Блок (bg-primary/5, border-primary/20) с:
- "Оценочная стоимость" + Badge "Предварительно"
- **~{formatCurrency(estimatedPrice + deliveryCost)}**
- При скидке: "(со скидкой X%: ~{formatCurrency(price * (1 - discount/100))})"
- Hint: "Финальную цену предложат кондитеры. Согласование — в личном кабинете после ответа."

---

### Success Screen (после отправки)

Показывается вместо шага 7 при `submitted === true`:

```
┌──────────────────────────────────┐
│         🎉 (PartyPopper icon)     │
│                                  │
│  Запрос отправлен N кондитер(ам)!│
│                                  │
│  Каждый кондитер получил ваш     │
│  проект торта [со скидкой X%].  │
│  Ожидайте предложений с ценой    │
│  в течение 48 часов.             │
│  Согласование и оплата — в       │
│  личном кабинете.                │
│                                  │
│  ┌─────────────────────────┐     │
│  │ Кому отправлено:        │     │
│  │ [avatar] Сладкая уезд.  │     │
│  │   ⏳ Ожидает ответа     │     │
│  │ [avatar] Татьяна-К.     │     │
│  │   ⏳ Ожидает ответа     │     │
│  └─────────────────────────┘     │
│                                  │
│  [Перейти в личный кабинет →]    │
│  [Закрыть]                       │
└──────────────────────────────────┘
```

---

## 💰 ФОРМУЛА РАСЧЁТА ЦЕНЫ

```
estimatedPrice = 1500                              // базовая цена (8 порций)
               + base.priceBase                    // + основа (0-400 ₽)
               + filling.price                     // + начинка (0-400 ₽)
               + coating.price                     // + покрытие (0-500 ₽)
               + SUM(decorations[].price)          // + декор (мультивыбор)
               + MAX(0, servings - 8) × 180        // + доп. порции

deliveryCost   = calculateDelivery(estimatedPrice)  // 0 при ≥3000₽, иначе 300+50/км
                 if deliveryType === "delivery", else 0

total          = estimatedPrice + deliveryCost
               × (1 - discountPercent / 100)       // если запрошена скидка
```

**Пример:**
- Свадьба + Бисквит (0) + Шоколадный ганаш (350) + Мастика (500) + Сахарные цветы (800) + Сусальное золото (600) + 30 порций
- 1500 + 0 + 350 + 500 + 800 + 600 + (30-8)×180 = 1500 + 2250 + 3960 = **7710 ₽**
- Доставка: бесплатно (≥3000 ₽)
- Без скидки: **7710 ₽**
- Со скидкой 10%: **6939 ₽**

---

## 🔄 FLOW (пользовательский путь)

```
Открытие (клик "Конструктор" на главной)
  │
  ▼
Шаг 0: Мероприятие ────選birthday──────▶ "Далее"
  │                                      │
  ▼                                      ▼
Шаг 1: Основа ──────選sponge────────▶ "Далее"
  │                                      │
  ▼                                      ▼
Шаг 2: Начинка ────選chocolate_ganash─▶ "Далее"
  │                                      │
  ▼                                      ▼
Шаг 3: Покрытие ───選mastic─────────▶ "Далее"
  │                                      │
  ▼                                      ▼
Шаг 4: Декор ────選[berries, macarons]─▶ "Далее" (опционально)
  │                                      │
  ▼                                      ▼
Шаг 5: Диета ────選[] (skip)────────▶ "Далее" (опционально)
  │                                      │
  ▼                                      ▼
Шаг 6: Доставка ──Москва, 2026-08-20, delivery, 10 порций──▶ "Далее"
  │                                      │
  ▼                                      ▼
Шаг 7: Резюме ────選 2 кондитера + скидка 10%────▶ "Отправить запрос (2)"
  │
  ├── Если НЕ залогинен → AuthModal открывается
  │                        toast.info("Войдите, чтобы отправить запрос")
  │
  └── Если залогинен → создание Negotiation для каждого кондитера
                       ├─ negotiation.status = "pending_confectioner"
                       ├─ negotiation.expires_at = NOW + 48 часов
                       ├─ negotiation.originalRequest = {весь проект торта}
                       ├─ negotiation.discountRequested = true/false
                       └─ toast.success("Запрос отправлен N кондитер(ам)!")

Success Screen
  ├── Список кондитеров с аватарами + "⏳ Ожидает ответа"
  ├── "Перейти в личный кабинет →" (navigate dashboard-customer)
  └── "Закрыть" (handleClose → resetCakeBuilder)
```

---

## 🔧 ВСПОМОГАТЕЛЬНЫЕ КОМПОНЕНТЫ

### `StepContainer`
```typescript
function StepContainer({ title, subtitle, children }: {
  title: string;
  subtitle: string;
  children: React.ReactNode;
})
```
Обёртка для каждого шага: h3 title + p subtitle + children

### `OptionCard`
```typescript
function OptionCard({ selected, onClick, children }: {
  selected: boolean;
  onClick: () => void;
  children: React.ReactNode;
})
```
Карточка опции:
- `selected` → border-primary, bg-primary/5, ring-2 ring-primary/20
- `!selected` → border-border, hover:border-primary/30
- Клик → onClick

### `FillingSlicePreview`
Импортируется из `@/components/cake-slice/cake-slice-visualizer`
- Визуализирует 3D-разрез торта на основе названия начинки
- Размер: 56px (на шаге 2) или 64px (на шаге 7)
- Показывает слои: бисквит + начинка + покрытие

### `SummaryRow`
```typescript
function SummaryRow({ label, value }: { label: string; value?: string })
```
Строка в сводке: "Label: value"

---

## 🔐 ПРАВА ДОСТУПА

- **Гость:** может открыть конструктор, пройти все 8 шагов, но при отправке → AuthModal
- **CUSTOMER:** может отправить запрос → создаётся inquiry + negotiations
- **CONFECTIONER:** получает inquiry в дашборде → quote/accept/decline
- **ADMIN:** видит все inquiries и negotiations через admin dashboard

---

## 📱 RESPONSIVE

| Breakpoint | Grid (steps 0, 4) | Grid (steps 1-3, 5) | Max-width |
|---|---|---|---|
| Mobile (`< md`) | 2 columns | 1 column | full width |
| Desktop (`>= md`) | 3 columns | 2 columns | max-w-2xl (steps 6, 7) |

Dialog: `max-w-4xl max-h-[95vh]` — адаптируется под экран
Content area: `overflow-y-auto` — скролл при overflow
Footer: `shrink-0` — всегда виден (не скроллится)

---

## 🔌 ИНТЕГРАЦИЯ С SUPABASE (v2.0)

### При отправке запроса (handleNext, step 7):

```typescript
// v1.0 (текущая реализация в cake-builder-dialog.tsx):
useAppStore.setState({ negotiations: [...negotiations, ...newNegotiations] });

// v2.0 (через useSubmitInquiry hook):
useSubmitInquiry.mutate({
  draftId: "draft-123",               // ID автосохранённого черновика
  confectionerIds: ["conf-1", "conf-2"],
  estimatedPrice: 7710,
  requestDiscount: true,
  discountPercent: 10,
  discountComment: "Постоянный клиент",
});
// → POST /api/inquiries
//   → Создаёт inquiry (snapshot проекта торта)
//   → Создаёт N negotiations (по одной на кондитера)
//   → Помечает draft как is_submitted = true
//   → Отправляет уведомления кондитерам через Edge Function
```

### DB таблицы (миграция 0004_cake_builder_chat.sql):
- `cake_builder_options` — опции (bases, fillings, coatings, decorations, dietary, event_types)
- `cake_builder_drafts` — автосохранение черновика (step, is_submitted)
- `inquiries` — отправленные запросы (status: open → converted/expired)
- `negotiations` — переговоры (status: pending_confectioner → quoted → accepted/declined)
- `negotiation_revisions` — история изменений цены
- `negotiation_messages` — realtime чат в переговорах

### Hooks (use-cake-builder.ts):
- `useCakeBuilderOptions(category)` — опции из БД
- `useCakeBuilderDraft()` — автосохранение черновика
- `useSubmitInquiry()` — отправка запроса
- `useNegotiations({ role: "customer" | "confectioner" })` — список переговоров
- `useNegotiationMessages(negotiationId)` — realtime сообщения

---

## 🎨 ВИЗУАЛЬНЫЙ СТИЛЬ

| Элемент | Цвет | Размер |
|---|---|---|
| Dialog background | `/builder-bg.png` + `bg-background/90` overlay | full |
| Header gradient | `from-primary/10 via-accent/20 to-primary/10` | px-6 py-4 |
| Cake icon | `bg-primary text-primary-foreground` | h-10 w-10 rounded-full |
| Progress bar (active) | `bg-primary` | h-1.5 flex-1 |
| Progress bar (inactive) | `bg-border` | h-1.5 flex-1 |
| Selected OptionCard | `border-primary bg-primary/5 ring-2 ring-primary/20` | — |
| Unselected OptionCard | `border-border hover:border-primary/30` | — |
| Price Badge | `variant="secondary" text-[10px]` | — |
| Discount block | `border-amber-300 bg-amber-50/50` | p-3 |
| Price estimate | `bg-primary/5 border-primary/20` | p-3 |
| Footer | `bg-card/90 backdrop-blur` | border-t p-4 |
| Next button | `bg-primary text-primary-foreground` | default |
| Back button | `variant="ghost"` | default |
| Close (X) | `hover:bg-accent` | h-8 w-8 rounded-full |

**Шрифты:**
- Title: `font-display` (Brokgauz & Efron Italic)
- Body: default (Geist Sans)
- Price: `font-display font-bold`

---

## 📊 СВОДНАЯ ТАБЛИЦА ВСЕХ ШАГОВ

| # | Название | Обяз. | Выбор | Опций | Цена влияет | canProceed |
|---|---|---|---|---|---|---|
| 0 | Мероприятие | ✅ | single | 6 | ❌ | eventType != null |
| 1 | Основа | ✅ | single | 6 | ✅ (+0-400 ₽) | base != null |
| 2 | Начинка | ✅ | single | 8 | ✅ (+0-400 ₽) | filling != null |
| 3 | Покрытие | ✅ | single | 6 | ✅ (+0-500 ₽) | coating != null |
| 4 | Декор | ❌ | multi | 8 | ✅ (сумма) | true (всегда) |
| 5 | Диета | ❌ | multi | 6 | ❌ | true (всегда) |
| 6 | Доставка | ✅ | form | 5 полей | ✅ (delivery + порции) | city + date + type |
| 7 | Резюме | ✅ | multi | N кондит. | ✅ (скидка) | selectedConfectioners > 0 |
| **Итого:** | 8 шагов | 5 обяз. | 3 single + 2 multi + 1 form + 1 multi | **45 опций** | | |

---

## 🔄 АВТОСОХРАНЕНИЕ (v2.0)

В v2.0 черновик автосохраняется через `useSaveDraft()`:
- При смене шага → `updateCakeBuilder({ step })` → debounce 2 сек → `useSaveDraft.mutate(draft)`
- При закрытии без отправки → черновик сохраняется, можно продолжить позже
- При отправке → `is_submitted = true`, черновик больше не редактируется
- При входе в конструктор → `useCakeBuilderDraft()` загружает последний незавершённый черновик

---

## 📐 ПОТОК ДАННЫХ

```
CAKE_BUILDER_OPTIONS (mock-data.ts)
       │
       ▼
CakeBuilderDialog (component)
       │
       ├── useAppStore.cakeBuilder (state, 8 шагов)
       │   ├── updateCakeBuilder() → изменение полей
       │   ├── resetCakeBuilder() → сброс при закрытии
       │   └── cakeBuilder.step → управление текущим шагом
       │
       ├── calculatePrice() → estimatedPrice
       │   ├── base.priceBase
       │   ├── filling.price
       │   ├── coating.price
       │   ├── decorations[].price (sum)
       │   └── (servings - 8) × 180
       │
       ├── calculateDelivery() → deliveryCost
       │
       ├── canProceed() → boolean (зависит от step)
       │
       ├── handleNext()
       │   ├── step < 7 → updateCakeBuilder({ step: step + 1 })
       │   └── step === 7 → submit
       │       ├── !isAuthenticated → AuthModal
       │       └── isAuthenticated → создание negotiations
       │           ├── v1.0: useAppStore.setState({ negotiations })
       │           └── v2.0: useSubmitInquiry.mutate()
       │               ├── INSERT inquiry (snapshot)
       │               ├── INSERT negotiations × N
       │               ├── UPDATE draft (is_submitted = true)
       │               └── send-notification Edge Function
       │
       └── handleBack() → updateCakeBuilder({ step: step - 1 })
```

---

## 🎯 КЛЮЧЕВЫЕ ОСОБЕННОСТИ

1. **8-шаговый wizard** с прогресс-баром — пользователь всегда видит, где он
2. **3D-превью среза торта** через FillingSlicePreview — уникальная фича
3. **Мультивыбор декора** (до 8 опций) и **диет** (до 6 опций)
4. **Динамический расчёт цены** в реальном времени при каждом выборе
5. **Мультивыбор кондитеров** — отправка запроса нескольким кондитерам одновременно
6. **Запрос скидки** (0-30%) с комментарием — каждый кондитер видит и решает
7. **Автосохранение черновика** (v2.0) — можно закрыть и продолжить позже
8. **48-часовой таймер** — inquiry истекает через 48 часов
9. **Success screen** — список кондитеров с аватарами и статусом "⏳ Ожидает ответа"
10. **Responsive** — grid адаптируется (2→3 columns, 1→2 columns)
11. **Background image** — `/builder-bg.png` с overlay для читаемости
12. **Security** — незалогиненный пользователь может пройти все шаги, но отправка требует auth
